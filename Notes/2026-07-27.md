### Point 1330


A faire :

- HLD : historique des changements

- stretch goal : gestion centralisee d'identités et secrets

- attaques sur bgp

snakeoil: certificat utilisé dans des exemples et dont la clé privée est publique (donc a changer)

- restrictedshell

- algorythme de schor et recommendations du Nist sur la PQC
    - selon le nist etat de vuln des algos acutls
    doit couvrir asymetrique et symetrique

postgresql :
    serveur de BDD, clustering possible, plusieurs ecritures en meme temps possible
    replication possible, solutions de backup tres poussée

sqlite : 
    un fichier tres bien pour petite echelle, pas d'ecriture simultanee

bonus : lire la doc sqlite et des livres sur postgresql
    important de comprendre les systemes de bdd

partage du dossier BDD de quentin a regarder

rapport :
    annexe : HLD
    presenter le HLD
        1er chapitre : pk ce choix de cible la
            discussion de simuler un reseau a l'arrivee d'un specialiste securité
                eg. application : interaction user et compostante backend (BDD) donc oblige a exercer du jugement dans les evolutions techniques
        1 chapitre par evolution
            avant ces chapitre faire une partie sur les tests fonctionnels sur les machines

ajouter serveur de backup au HLD
    sera sur un reseau dédié avec une interface partagée avec le pare feu
    techno : borg

- chercher ce qu'est "borg" et dire s'il vaut mieux des backups push ou pull

- chercher berkely packet filter et EBPF

bonus : creer un depot et stocker 2 fichier word via gitlfs
    cloner le depot et expliquer comment gitlfs fonctionne

- connection https 443 regarder certificat
    - debugger la connection locale avec curl

- transformer le script start_all.sh en demon systemd

- dumper depuis la BDD avec `psql` tous les droits de l'user "mur"

devoirs :

- VM pfsense
    - noter quelles interface j'ai au départ 
    - si if fonctionnelle désactivée prouver que l'if est desactivée
        mettre sur la preuve l'output de date dans la même commande (eg. ip r && date) et prendre capture
    - prouver que tt fonctionne

- Rajouter une machine avec Borg
    - un backup via borg de la VM de BDD toutes les heures et verifier comment backup une BDD postgresql
        "est ce qu'on peut juste copier les fichiers"
    - dans un reseau separé
    - utiliser la PKI
        rapport pratique opé standard

- chercher comment revoquer l'acces a une clé


decalage point mercredi a 1030


stretch goal  :
    - installation et configuration de fail2ban sur toute les machines
    - installation d'une VM security onion dans son propre sous reseau
    - installation et configuration de suricata sur pfsense
    - deversement des logs fail2ban et suricata vers security onion




### CR 

Hello, 

Voici le compte rendu du travail a faire de ma part pour mercredi.


Recherches : 

- chercher ce qu'est une attaque sur bgp
- chercher "restrictedshell"
- chercher algorythme de schor et les recommendations du Nist sur la PQC (pour le chiffrement symétrique et asymétrique)
- regarder le partage du dossier BDD
- chercher ce qu'est "borg" et dire s'il vaut mieux des backups push ou pull
- chercher berkley packet filter et EBPF
- chercher comment revoquer l'acces a une clé

Pratique :

- Ajouter une section "historique des changements" dans le HLD
- Ajouter un serveur de backup Borg au HLD qui sera sur un sous réseau dédié interfacé avec le pare feu

- debugger la connection locale avec curl
- regarder le certificat du site lors d'une connection https

- transformer le script start_all.sh en demon systemd
- dumper depuis la BDD avec `psql` tous les droits de l'user "mur"

- Créer une VM pfsense
    - noter quelles interface sont présentes au départ 
    - si interface fonctionnelle désactivée alors prouver que l'interface est desactivée
        eg. : mettre sur la preuve l'output de date dans la même commande (eg. ip r && date) et prendre capture
    - prouver que tout fonctionne via des tests

- Rajouter une machine avec Borg
    - un backup via borg de la VM de BDD toutes les heures et verifier comment backup une BDD postgresql (regarder si on peut "juste" copier les fichiers)
    - la VM doit etre dans un sous reseau separé (uniquement en lien avec le pare feu
    - la connection a la VM doit utiliser la PKI
        
stretch goal  :
    - installation et configuration de fail2ban sur toute les machines
    - installation d'une VM security onion dans son propre sous reseau
    - installation et configuration de suricata sur pfsense
    - deversement des logs fail2ban et suricata vers security onion

