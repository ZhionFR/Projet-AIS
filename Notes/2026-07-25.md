Recherches : TODO
- Faire une presentation detaillée des commandes et utilitaires dans POSIX : Lier les commandes documentées dans /Docs/commands.md avec les commandes standards POSIX 
    - Toutes les commandes "standard" : cat, cp, ls, chmod, mv, mkdir,...

- Refaire le scenario de l'attaque de spoofing a plus grande echelle (millions d'utilisateurs) qui marche meme en https
    - https : protege le contenu, pas la destination
    - d'apres l´ indice : incident de 2019, le gouvernemt force la population a installer un certificat malveillant pour pouvoir lire toutes les données qui transitent
        aucun warning comme certificat valide, refus = pas d'internet, "légal" vu que l'état oblige 

- Idem petite echelle: dans un hotel qui a un wifi avec un portail captif, comment faire pour se connecter au portail captif en https avec un certificat qui s'appelle snakeoil
        - Question 1 : peut on faire du vol d'identifiant, si oui comment ?
            - les gens se connectent naivement avec un certificat auto signé (pas rare pour un portail captif) 
            - on intercepte le handshake wifi et on redirige vers notre portail captif autosigné

- chercher "restrictedshell" et le meme scenario mais coté serveur
    - RCE - reverse shell - redirection du traffic interne vers la machine compromise - reverse shell dans reseau interne depuis la machine compromise

- regarder acronyme 'TOFU' : ce qu'il veut dire, les risques associes et les mitigation les plus simples
    - TOFU : Trust On First Use
        risques : 1ere clé est malveillante/connection interceptée
        mitigation : verification du fingerprint (ssh-keyscan)

- recherche sur le fait de retrouver la cle privee a partir de la cle publique
    - expliquer pour une cle RSA ce qu'implique la methode et le niveau de risque
        - pas possible de craquer une clé RSA 2048bits avec les techologies actuelles
        - factorisation seule sans ordi quantique n'est pas un facteur de risque

    - chercher le Chiffre de vigenere (max 30min) et expliquer quelle est la difference fondamentale entre les chiffrements DSA/ed25519/... et les plus anciens (enigma, cesar,..)
        - chiffre de vigenere : cesar en decalant a chaque lettre 
        - difference : vieux = substitution, algorithme connu = cassable
                       nouveau = asymétrie mathématique, algos public mais sûrs
        
    - chercher si AES est plus proche d'enigma ou de ed25519 et pourquoi
        - plus proche de enigma car série d'opération de substitution 
            les clés sont différentes et les opérations n'ont pas de "sens" (on ne peut pas vérifier le résultat sauf en ayant fait toutes les opérations : plus dur a casser)

- chercher ce qu'est la CNCF et les implications sur les standards
    - Cloud Native Computing Foundation
        - Google, microsoft, amazon,...
        - Standardisation massive (conteneurisation notamment)
            -> Complexité elevée

- expliquer pourquoi postgresql
    - plus sécurisé (mdp dans la config)
    - directement en reseau TCP/IP (pas dans un fichier) : plus simple a config sur un autre serveur
    - Open source -> modulable si besoin/bug et probablement tres documenté en conséquence

- presenter ce que sont des stateful et stateless firewall
    stateful : maintien une table de connection et autorise les réponses aux requetes autorisées
    stateless : plus strict, besoin de bien configurer le pare feu si on veut recevoir les réponses

- lire l'architecture et fonctionnement de iptables (1h)
    - chercher les differentes tables (eg. MANGLE)
    RAW (config brute), MANGLE(modif paquets), NAT(translation d'adresse), FILTER(filtrage)
    flux paquet complet :

Interface reseau
    ↓
Kernel reçoit
    ↓
┌─────────────────────────────────────┐
│ raw table (PREROUTING)              │
│ Pas de règles typiquement           │
│ → Continue                          │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ mangle table (PREROUTING)           │
│ Modifier paquets ? (ex: TOS bits)   │
│ → Continue                          │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ nat table (PREROUTING)              │
│ DNAT ? (redirection destination)    │
│ → Continue                          │
└─────────────────────────────────────┘
    ↓
Routage décision
(ce paquet est pour moi ?)
    ↓ OUI
┌─────────────────────────────────────┐
│ mangle table (INPUT)                │
│ Modifier paquets ?                  │
│ → Continue                          │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ filter table (INPUT) ← CRITICAL     │
│                                     │
│ Rule 1 : Port 22 ACCEPT ?           │
│   Protocol=TCP && Port=22 ?         │
│   YES → ACCEPT                      │
│   Jump to end, paquet accepté       │
└─────────────────────────────────────┘
    ↓
Application (sshd)
    
- idem nftables (30min) puis regarder les différences entre iptables et nftables
    - tables sont user defined
    - plus de logging, plus recent = communauté croissante mais pas autant de doc
    - nftables unifie ipv4 et ipv6

- chercher application AWARE firewall
    - analyse le reseau usuel et detecte les anomalies

- regarder `netfilter`
    - iptables et nftables sont des interfaces cli de netfilter
    - fonctionne avec des hooks reutilisés par iptables/nftables

- regarder ICMP
    - protocole de diagnostique reseau (couche network)
    - ping (type=0), redirect, echange de timestamps,
    - pas de port
        - types (0-255)

- dans la VM SRV-1, enumerer les dossier dans `/proc/sys/net/ipv4` et expliquer ce qu'ils sont
    - config reseau ?
        eg : ip forward (1: activé) qu'on peut passer à 0 si non nécessaire
            - Il doit exister un script de hardening qui mets les options les plus secure

- Lire l'article envoyé par simplex aujourdh'hui

- chercher `gitlfs` : est ce une bonne idee de versionner des BLOB sur github
    - Git large file storage
    pas bon de versionner des gros fichiers : cher et souvent contenant des données sensibles (ex : BDD)



