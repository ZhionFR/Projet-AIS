Recherches documentaires :
- **le dilemne du prisonnier**
	- Pas d'interet majeur a se trahir 
	- Contrat basé uniquement sur la confiance
- **le logiciel `linpeas`**
	- tres utile : choix de privilegier le temps sur les pare feu
- **ce qu'est un `setuid` (10min max) et pourquoi c'est dangereux**
	- permet notamment de lancer un processus en root, donc une manoeuvre non maitrisee et risquee
- **comment utiliser sftp**
	- connection via le tunnel ssh
	- telecharger vers hote : !get /path/to/file 
		- !reget (-r si dossier) /path/to/file (si interrompu)
	- telecharger depuis hote : !put (-r) /path/to/file
		- !reput /path/to/file
	- mget et mput pour multiple fichiers
- **comment passer un script actif en background ou en sommeil**
	- background : ajouter *&* à la fin de la commande
	- sommeil : j'ai juste trouvé "sleep" en bash...
		- CTRL + Z
- **les fichier fifo (5min max) et commandes associees**
	- C'est le fichier pipe je vais me tuer mdr
	- on peut en créer avec mkfifo
- **vim vs emacs**
	- pages wikipedias des deux
	- comparaison des outils en pratique (sur le pc)
- **optionnellement regarder "linux from scratch"**
	- pas fait

Pratique :
- **analyse adversariale du contenu du contrat pédagogique**
	- J'en ai parlé a un camarade qui m'a dit que c'était bien (le contenu lui semblait cohérent avec ce qui a été demandé)
- **un document HLD (high level design) du projet final qui integrera un bastion dans l'architecture finale (évaluer les solutions warpgate, zebastion et wallix)**
	- faire parvenir le document en amont du point de jeudi prochain
- **dans `/etc/ssh` :**
	- **differencier config client et serveur**
		- serveur : sshd
		- client : ssh
	- **pour la config client : faire un test de si on met une config à `/etc/ssh/ssh_config` et une config differente dans `/home/user/.ssh/config` laquelle est prioritaire**
		- config user est prioritaire
			  > La config "au niveau serveur" est celle par défaut
- **quels sont 2 fichiers les plus importants dans la config utilisateur (dans `/home/user/.ssh/`)**
	- `authorized keys`et `id_ed25519` car les clés sont importantes
- **une analyse sur les permissions des clés ssh dans /home/user/.ssh/**
	- seules perms owner sur clé privée, perms de lecture pour le reste
- **trouver quelles sont les permissions les plus relaxées que ssh accepte**
	- si perm lecture autre que owner sur une clé privée, ssh refuse de l'utiliser
- **versionner les scripts bash dans le dêpot github et ajouter le script au PATH de l'utilisateur sans utiliser sudo/root, il faut que l'ajout soit durable.**
```bash
thomas@SRV-1:~$ mkdir bin  
thomas@SRV-1:~$ mv start_ngrok bin/start_ngrok.sh  
thomas@SRV-1:~$ mv start_webapp bin/start_webapp.sh  
thomas@SRV-1:~$ mv start_tmux bin/start_tmux.sh  
thomas@SRV-1:~$ echo 'export PATH="$HOME/scripts:$PATH"' >> ~/.bashrc  
thomas@SRV-1:~$ source ~/.bashrc
``` 
- **determiner où les scripts sont traditionnellement mis dans le /home**
	- `~/bin/` ou `~/.local/bin/`
- **refactorer les scripts tmux pour créer des terminaux au lieu de sessions**
	- TODO
- **se documenter sur iptables (4h environ) et tester de configurer le pare feu du serveur pour qu'il n'autorise que les connections entrantes sur les ports 2222 et 5000 via tcp, ainsi que rediriger le port 80 vers 5000 (et l'ouvrir en tcp sur le pare feu)**
	- **installer `iptables-apply`**
		- TODO
- **chercher à déterminer si virt manager supporte des interface virtuelles pour bridger des VMs entre elles sur des interfaces spécifiques**
	- Renvoie à la galère du `nmcli` > je suppose que oui mais pas avec parrot (ne supporte pas certaines architectures comme i386, peut etre que des sécus supplémentaires ont été mises / ou à cause de la carte wifi mais elle semble compatible)