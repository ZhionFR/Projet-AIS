Promtail plus supporté : évolutions névessaire : passage a alloy

installer alloy

lancer avec fichier config

"http server lancé"

verif via nmap

port filtré

root@SRV-3:/home/thomas# iptables -A INPUT -p tcp --dport 4318 -m conntrack --ctstate NEW -j ACCEPT


install alloy sur client

alloy debugging interface

echo "test" >> /var/log/fail2ban.log

alloy  OK

ssh NL pour interface debug



les interfaces reseaux etaient en NAT : c'est OK maintenant


