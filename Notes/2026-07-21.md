### Recherches documentaires : 
- Qu'est ce que `POSIX`
    - standards de compatibilité entre les OS

- Trouver un scénario dans lequel un attaquant pourrait rediriger un requête vers un serveur (envoyée avec l'adresse IP du serveur) vers ailleurs et trouver un protocole utilisé pour faire du transfert de données en prenant en compte de scénario
    - dire au réseau que le chemin pour atteindre le serveur est plus court par lui et donc recevoir la requête. Il suffirait de virtualiser un routeur et le connecter au réseau
    - Protocole : TLS (https, IPsec, DNSSEC,...) 

- Chercher comment donner un accès ssh sans donner accès au shell
    - `ssh -t user@host app` permet de specifier que l'utilisateur utilisera cette application specifique en ce connectant via ssh

- Regarder le contenu de `knownhosts`dans la config ssh
    - liste des clés publiques des serveurs ssh auquel le client s'est connecté pour proteger des attaques "man-in-the-middle"

- Rechercher des techniques de monitoring projet et estimation de budget à la livraison (max 30min). *mot clé : CPI*
    - EVM : Earned Value Management
        - PV/Planned value  
        - EV/Earned value 
        - AC/Actual cost
        - CPI/Cost performance index = EV/AC : si >1 alors c'est bon
        - BAC/Budget at completion
        - EAC/Estimate at competion = BAC / CPI dans la majorité des cas

- Faire des recherches sur http par udp
    - HTTP/3 utilise udp pour aller plus vite sans bloquer les autres flux de transmissions (pas de handshake : pas de "timeout")
    - on sacrifie de la précision pour gagner en efficacité

- Expliquer la différence entre OLTP et OLAP
    - OLAP : données agrégées (créer un rapport ou analyser des données complexes)
    - OLTP : transaction de BDD (mettre à jour l'inventaire ou gérer des comptes clients)



### Travaux documentaires : 
- Construire un répertoire de commandes et exécutables avec une page comprenant les commandes utilisées (contexte, pourquoi, comment)
    - fait à /Docs/commands.md

- Preparer une présentation sur le durcissement et ce que c'est
    - OK. /Docs/pres_hardening.md


### Travaux pratiques : 
- Snapshot de la VM avec `$sudo cp /var/lib/libvirt/images/SRV-1.qcow2 /home/zhion/Projet-AIS/backup/SRV-1_$(date +%F).qcow2` et `virsh dumpxml SRV-1 > SRV-1_$(date +%F).xml`
    - test de restauration en copiant le fichier dans l'autre sens OK
        test : installer les outils de virtualisation sur une autre machine (vm parrotos sur un autre pc en l'occurence) et restaurer la vm à partir des 2 fichiers, la config ssh, le serveur web et les clés de connection étaient bien sauvegardées.
    - Backup disque non dans git car trop volumineux
        - Code source du serveur web est toujour sauvegardé et .xml est plus important car config de la VM

- Écrire un script qui crée un fichier fifo (`mkfifo`) et qui le remplit via la commande `dd` qui rajoute des `0` dans le fichier, on doit pouvoir connaître la vitesse d'écriture.
    - dd refuse d'écrire dans le fichier sans que quelque chose écoute dessus
      cat breaking_the_matrix > /dev/null &
      dd if=/dev/zero of=breaking_the_matrix bs=4M status=progress
      11974737920 bytes (12 GB, 11 GiB) copied, 24 s, 499 MB/s
    - `yes` refuse aussi d'écrire
    - Conclusion : on ne peut pas casser linux avec un pipe :( ou alors je suis trop mauvais

- Utiliser et expliquer la commande `free`
    - Affiche des détails sur la mémoire physique et swap

- Tester `ssh-keygen` sans paramètres
    - utilise id_25519 par défaut (j'ai failli overwrite ma clé avec ces conneries)

- Refactorer les scripts tmux pour créer des terminaux au lieu de sessions
    - OK. voir /Scripts/start_all.sh


### Évolutions logicielles : 
- Passer à Vim pour la prise de notes
    - OK. Vim installé aussi sur le serveur :thumbs_up:

- Passer la webapp de sqlite à postgresql en local sur la VM avec une configuration durcie, documentation des tests et expliquer l'interêt de cette migration (et dans quels cas on ne la ferait pas)
    - OK. Tests concluant. Le script de migration est la seule trace restante de sqlite.


