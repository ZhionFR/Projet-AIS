### Logs de recherches :

Recherches : 
	hyperviseur
		https://fr.wikipedia.org/wiki/Hyperviseur
			schéma clair
			explication des 2 types d'hyperviseurs
	paravirtualisation
		https://edera.dev/stories/what-the-f-ck-is-paravirtualization#
			clair et précis
			schéma explicites
	virtualbox vs vmware vs qemu
		IA search assist :

| Hypervisor | Type            | Description                                                                              | Best Use Cases                                      |
| ---------- | --------------- | ---------------------------------------------------------------------------------------- | --------------------------------------------------- |
| VirtualBox | Type-2          | A user-friendly hypervisor developed by Oracle, suitable for various OS.                 | Desktop virtualization, testing                     |
| VMware     | Type-1 & Type-2 | Offers both type-1 (bare-metal) and type-2 (hosted) virtualization options.              | Enterprise environments, server virtualization      |
| QEMU       | Type-2          | An open-source hypervisor that can function as a type-1 hypervisor when paired with KVM. | **Development, testing, and server virtualization** |
		https://www.diskinternals.com/vmfs-recovery/qemu-vs-virtualbox-vs-vmware/ 
			Compare déjà les 3
			bien détaillé
	proxmox
		https://www.proxmox.com/en/
			Ils expliquent ca eux meme :
				*Proxmox Virtual Environment is a complete open-source platform for enterprise virtualization* 			
	ORM (Object-Relational Mapping)
		Je connaissais déjà donc avoir le nom suffit

### Point 1030

##### Prepa :

fait :
	github créé
	necessaire de virt
	creer VM
		debian
		connection ssh
		interface bridgée avec wifi de l'host (il s'avere que non)
	recherches faites
	app faite (avec claude code)
		En local à http://127.0.0.1:5000/

reste à faire
	une fois que l'app fonctionne, la deployer sur la VM et modifier le fichier HOSTS pour pouvoir y acceder "lechatdethomas.michel.local"

#### Notes

redonner github (fait)

update github app
revue contrat peda PRIORITAIRE (adversariale avec claude)
setup ssh via ngrok pour quentin
demultiplexeur de terminaux 
	installer temux
		apprendre a se connecter, ouvrir 2 fenetres, se deco de ssh et se reco a la meme session
creer un user pour quentin (demander dans CR)
	reqs : doit etre impossible de s'y connecter par mdp (seulement par clé)
se connecter au meme terminal temux avec les 2 users
packager l'application pour la faire tourner sur la VM
	git clone
	lancer l'app sur la VM et se deconnecter de la VM
		ne pas en faire un demon systemd
		seulement via temux
		et demontrer exec en background
		verifier depuis le meme terminal que l'application se lance et qu'on peut lancer une autre commande (temux)
	explorer intercface github (20min)
	creer un alias sous bash pour `git commit -a` 
interface bridgée
	prouver qu'elle est bridgée

#### CR

Re-bonjour,

Voici le CR de la réunion d'aujourd'hui 10h30 où vous étiez présent.

Nous avons commencé par revoir le travail fait depuis la dernière fois. La revue du travail fait à révelée que l'interface n'était pas bridgée comme demandée.

Il a été demandé à ce que Quentin MALLET envoie une clé publique ssh à Thomas MICHEL au plus vite.

Il sera à faire pour la prochaine fois pour Thomas MICHEL :
	- bridger l'interface (et le démontrer)
	- explorer l'interface de github (20min max) et y déposer l'application web
	- faire revoir le contrat pédagogique extérieurement
	- setup ssh via ngrok pour accéder à la VM "SRV-1"
	- créer un user "quentin" accessible uniquement par clé ssh (sans mdp)
	- installer temux, apprendre à s'en servir et le mettre sur la VM
		- vérifier que 2 users peuvent utiliser un meme terminal via temux
	- cloner l'application web sur la VM via github
		- la faire tourner en background (sans en faire un demon systemd)
		- demontrer qu'on peut la lancer dans un terminal puis utiliser le terminal
		- pouvoir lancer l'app puis se deconnecter sans qu'elle s'arrete
	- créer un alias bash pour `git commit`


En vous souhaitant une bonne journée.

Cordialement,  
  
Thomas Michel