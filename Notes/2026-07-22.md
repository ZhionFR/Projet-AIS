### Travaux pratiques : TODO
- Via `iptables`, configurer le pare feu du serveur pour qu'il n'autorise que les connections entrantes sur les ports 2222 et 5000 via tcp, ainsi que rediriger le port 80 vers 5000 (et l'ouvrir en tcp sur le pare feu) - installer `iptables-apply`
    - Notes :
        Ne pas flush car ca casse docker
        PREROUTING se fait avant FILTER donc on filtre sur le port 5000 les requetes pour le port 80 (vu qu'on les redirige avant)
        bloquer INPUT sur tout sans mettre conntrack established/related bloque notamment les maj de software :/
    -Question :
        Pourquoi ne pas faire de DROP sur la table NAT (FILTER ne redirige pas donc ok mais NAT peut DROP ?)
        Peut-on préciser http/ssh plutot que juste tcp ? via une signature de paquet ou jsp


- Créer une 2e VM avec une interface bridgée avec la première et un utilisateur pour s'y connecter. Démontrer qu'on peut pinger une VM depuis l'autres (et que l'une des deux ne répond pas aux pings de l'autre) et que l'on peut se connecter en ssh. 
    - TODO

- Setup une PKI simple pour pouvoir se connecter avec une clé sans la mettre sur le serveur
    - TODO


### Pour le projet de HLD : DONE 
- Chercher ce qu'est un diagramme de séquence et en faire un pour les interactions utilisateur et un pour celles admin
- Faire un diagramme qui montre les périmètres de sécurité
- Faire une matrice de flux avec pour chaque entrée une explication des mesures de sécurisation mises en place
- Faire un diagramme réseau
    - Fait et envoyé
      MailID : 2607220927


### Si tout cela est fini avant jeudi : TODO
- Créer une 3e VM PfSense avec des interfaces bridgées pour la mettre entre les deux premières VM


### Question bonus : 
- Pourquoi les gens mettent du https sur des sites ou "l'accès public" est assumé?
    - Attaques MITM 
    - Tout le monde peut voir le flux des utilisateurs (qui consulte quoi)
    - Pas de TLS = pas de garantie de l'exactitude des donnés des deux côtés

