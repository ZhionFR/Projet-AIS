- écrire une PSSI (orientée service)
    - OK.
    
- bloquer youtube en local (resolvconf)
    - resolv.conf ? plutot /etc/hosts, avec resolv.conf on redirige vers un resolver qui filtre
    - avec hosts :  
```
#/etc/hosts
0.0.0.0  youtube.com
0.0.0.0  www.youtube.com
0.0.0.0  m.youtube.com
```

- bloquer youtube depuis le réseau
    - en utilisant 'Domain Override' on peut rediriger les requetes pour youtube.com vers 0.0.0.0 (par exemple)
- démontrer le bloquage puis le contourner (sans VPN/proxy)
    - On peut juste utiliser un DNS alternatif ou directement par IP


