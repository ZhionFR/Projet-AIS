### Point CR 1600

Recherches :
- Presentation detaillée des commandes et utilitaires dans POSIX
- Refaire le scenario de l'attaque a plus grande echelle (millions) qui marche meme en https
    - desc : tu travailles pour un état et tu veux que quand un max de gens du monde iront sur youtube on redirige vers un site alternatif
    - https ne l'empeche pas
    - un protocole "invisible"(habituellement manipulé par les fai) est utilisé
        - indice : kazaghztan
- Idem petite echelle :
    - desc : tu arrives alhotel pour une convention qui a lieu le lendemain. tu es pauvre et n'as pas le pass wifi de l'hotel. l'hotel a un wifi avec un portail captif, comment faire pour se connecter au portail captif en https avec un certificat qui s'appelle snakeoil
        - Q1 : peut on faire du vol d'identifiant, si oui comment ?
        - Q2 : sinon pk
- chercher "restrictedshell" et scenario mais cote serveur
    - fichie config
- regarder acronyme 'TOFU'
    ce que ca veut dire
    risques asocies
    mitigation les plus simples
- retrouver la cle privee a partir de la cle publique ? 
    - expliquer pour une cle RSA ce qu'implique la methode et est ce que c'est un risque negligeable/probalbe/important
    - chercher le Chiffre de vigenere (max 30min) quelle est la difference fondamentale avant DSA/ed25519/...
    - est ce que AES est plus proche d'enigma ou de ed25519 et pourquoi
- qu est ce que la CNCF et les implications sur les standards
- /Docs/commands.md = relier les commandes aux recherches sur POSIX


- faire un vrai snapshot avec virsh
    - utiliser la commande 'top' pendant le snapshot
- refaire la manip du pipe avec ecriture dans un socket unix a la place
    - script qui ecrit des 0x00 en boucle dans un socket
    - un autre qui lit depuis le socket
        on exec les deux et que se passe t il si on arrete d'écouter
- utiliser et expliquer `watch`
- expliquer pk postgresql


- presenter stateful et stateless firewall
- lire architecture et fonctionnement de iptables (1h)
Questions notées :
Pourquoi ne pas faire de DROP sur la table NAT (FILTER ne redirige pas donc ok mais NAT peut DROP ?)
    - chercher les differentes tables (eg. MANGLE)
Est ce que iptables fait le routage ou alors il intervient juste entre les étapes ?
    - 

- idem nftables (30min)
- iptables vs nftables

- rediriger 443 vers 5000 et tenter de se connecter en https

Pour le rapport :
dans une optique de durcissement systeme, nous avons choisi de faire tourner l'application pas en tant que root donc elle ne pouvait pas acceder au ports avant 1024. ce que nous avons fait c'est configurer un pare feu qui redirige le traffic du port 80 vers le port 5000. cela permet au utilisateurs de se connecter en http normalement

- chercher application AWARE firewall
- dans la VM, enumerer les dossier dans `/proc/sys/net/ipv4` et expliquer ce qu'ils sont
- regarder `netfilter`

- regarder ICMP

PKI :

creer une paire de clés ed25519 qui seront les clés CA.
mettre une copie de la clé publique sur les 2 serveurs et se servir de la clé privée pour signer les clés host des 2 serveurs et ma clé ssh
test : supprimer les entrées dans knownhosts des serveurs et authorizedkeys sur les serveur.
mettre a jour le demon ssh pour authoriser les CA

comment marche la connection a d'autres utilisateurs

Article envoyé par quentin a lire
