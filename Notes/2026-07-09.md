Installer
	virt-manager en gui
	libvirt
	kvm

à lire :
		hyperviseur
		paravirtualisation
		virtualbox vs vmware vs qemu
		proxmox
		ORM


app flask avec claude : gunicorn

centralisation des logs

SIEM :
	security onion
	graphana : pour exposer des metrics prometheus
	
je cree un depot github ou j'invite QMA et ou on construira le rapport au fur et a mesure.
je prends le plan et les notes et je mets tout dans un .md dans le github et je fais une analyse adversariale dessus (avec IA)
	je lui file le doc git et la spec AIS, le contexte de la simulation et dire "est ce que ce plan correspond aux attentes"
	préparer le 1er point à partir de la réponse pour le point de lundi pour phase gestion de projet
envoyer un CR des points :
	ce dont on a parlé et prochaines actions (d'ici le prochain point)


a avoir fait
	installer libvirt
	creer VM
		debian
		connection ssh
		interface bridgée avec wifi de l'host
			(possible de demander a claude)
			lire les docs (hyperviseur, qemu,...) avant
	PAS PLUS DE 2H EN RECHERCHE/ANALYSE : obj est de filtrer. logger les recherches avec "pk c'est intéressant"

si fini :
	commencer a bosser sur l'app
		developper d'abord en local sur l'host (via claude code)
			spec pour V0 :
				creer depot github (different)
				app en python developpee avec le framework flask, l'ORM peewee, sqlite en bdd
				l'app doit permettre d'acceder a une page web ou l'user peut saisir des messages et les enregistrer. le msg doit ensuite apparaitre sur la page web avec un pseudo choisit.
				un user qui recharge la page web doit voir tt les msg dans l'ordre chronologique : date de post / pseudo / contenu du msg
		stretch goal :
			une fois que l'app fonctionne, la deployer sur la VM et modifier le fichier HOSTS pour pouvoir y acceder "lechatdethomas.michel.local"

Si fini :
	pinger quentin 
	

contrat péda dans CR

oublis CR :
  Points 1030 (pas 1000)
  mention du log de recherches
  simpleX pour messages rapides/courts
