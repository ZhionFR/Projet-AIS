Travaux pratique : TODO
- faire un vrai snapshot avec virsh - utiliser la commande 'top' pendant le snapshot et le restaurer pour tester son efficacité
    - OK avec `top`

- refaire la manipulation du pipe avec écriture dans un socket unix à la place avec 2 scripts :
    - script qui ecrit des 0x00 en boucle dans un socket
    - un autre qui lit depuis le socket
    - on éxecute les deux et regarde si on arrete d'écouter ce qu'il se passe
        - erreur broken pipe

- utiliser et expliquer `watch`
    - execute la commande tout les X temps (pratique pour surveiller des flux rapides/constants

- rediriger le port 443 vers 5000 et tenter de se connecter en https (documenter les resultats et les expliquer)
    - je ne peux pas y accéder depuis un autre moyen que ngrok, en tapant 192.168.100.10 (ip de la VM sur l'interface linkée a l'host) j'y arrive pas (probablement skill issue)

- mettre en place une PKI sur les serveurs
    - OK
    - observer l'effet de la signature sur la connection aux utilisateurs
        - On signe la clé en précisant l'utilisateur donc c'est parfait

- faire la migration de la BDD sur SRV-2
    - durcir le pare feu avec uniquement traffic ssh et traffic postrgresql (uniquement depuis srv-1), drop par défaut pour le reste
        - OK : persistence des tables & de la BDD
    - Prouver que le site est toujours fonctionnel
        - Redémarrage complet des deux VM et le message de test est toujours présent :)

Snapshot : 0726_OK
