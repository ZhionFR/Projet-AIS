Recherches : 

- chercher ce qu'est une attaque sur bgp
    - detournement de traffic, bgp = protocole de routing
    - mitigations : rpki et whitelist/registre de route autorisées

- chercher "restrictedshell"
    - shell restreint a un emplacement, peut etre contourné si PATH et binaires mal configurés
    - on peut whitelister les binaires pour enlever less/vim/awk/... (rbash seul pas suffisant)

- chercher algorythme de schor et les recommendations du Nist sur la PQC (pour le chiffrement symétrique et asymétrique)
    - celui que je connaissais : algo de grover
    - recommendation du Nist : comme AES128 devient aussi efficace que AES64 on passe a AES256 :) pour symetrique
    - asymetrique : nouveaux algorythme ML-KEM echange de clé, ML-DSA et SLH-DSA pour les signatures

- regarder le partage du dossier BDD
    - TODO

- chercher ce qu'est "borg" et dire s'il vaut mieux des backups push ou pull
    - borg : sauvegardes chiffrees avec compression et gestion de retention
    - push : serveur envoie ses sauvegardes a borg, risqué car si serveur compromis alors attaquand a droit d'écriture sur borg
    - pull : borg tire les sauvegardes du serveur. mieux pour la sécurité

- chercher berkley packet filter et EBPF
    - BPF : VM pour filtrer les paquets reseau a base de tcpdump, programmes simples
    - eBPF : version etendue de BPF, permet exec de code sandboxé avec verificateur de terminaison. remplacement progressif d'iptables

- chercher comment revoquer l'acces a une clé
    - `ssh-keygen -kf revoked.krl -z <num de serie donné au moment de la signature>` puis reférencer ce fichier dans `sshd_config`

Pratique :

- Ajouter une section "historique des changements" dans le HLD
    - OK
- Ajouter un serveur de backup Borg au HLD qui sera sur un sous réseau dédié interfacé avec le pare feu
    - En validation

- debugger la connection locale avec curl
    - l'appli écoutait sur 127.0.0.1 au lieu de 0.0.0.0 :)

- regarder le certificat du site lors d'une connection https
    - erreur : SSL_ERROR_RX_RECORD_TOO_LONG

- transformer le script start_all.sh en demon systemd
    - OK; cf. Scripts/systemd_demon_start-all

- dumper depuis la BDD avec `psql` tous les droits de l'user "mur"
    - OK : 
mur_messages=# \du mur
                             List of roles
 Role name |                         Attributes
-----------+------------------------------------------------------------
 mur       | Superuser, Create role, Create DB, Replication, Bypass RLS

mur_messages=# \dp message
                              Access privileges
 Schema |  Name   | Type  | Access privileges | Column privileges | Policies
--------+---------+-------+-------------------+-------------------+----------
 public | message | table |                   |                   |
(1 row)

- Créer une VM pfsense
    - noter quelles interface sont présentes au départ 
        - toutes les "VLAN-Xx" ont étées crées pour la compartimentation PfSense, tout le reste (default, VLAN et VLAN10) existaient avant pfsense et seront drop dans l'architecture finale
    - si interface fonctionnelle désactivée alors prouver que l'interface est desactivée
        eg. : mettre sur la preuve l'output de date dans la même commande (eg. ip r && date) et prendre capture
        - voir screenrecording + screenshots
    - prouver que tout fonctionne via des tests
        - CA MARCHE JPP

snapshot : 0729_2am_pfsenseOK
screenrecording : ~/Videos/Screencasts/Screencast_20260729_020807.webm
Log avec date : 


- Rajouter une machine avec Borg
    - un backup via borg de la VM de BDD toutes les heures et verifier comment backup une BDD postgresql (regarder si on peut "juste" copier les fichiers)
    - la VM doit etre dans un sous reseau separé (uniquement en lien avec le pare feu
    - la connection a la VM doit utiliser la PKI
        
stretch goal  :
    - installation et configuration de fail2ban sur toute les machines
    - installation d'une VM security onion dans son propre sous reseau
    - installation et configuration de suricata sur pfsense
    - deversement des logs fail2ban et suricata vers security onion

