Recherches :

- infos publiques sur les prix des HSM
    - Thales nshield Solo commence à 6k€
    - neuf en général 5k€-50k€ - occasion descend jusqu'à 1k€
    - certains sont sur devis 

- ce qu'est un KMS par Azure, AWS, Google
    - HSM : equipement physique / KMS : service managé
    - AWS KMS : parc de HSM 
        - FIPS 140-3 niveau 3
    - Azure Key Vault : stockage de secrets
        - FIPS 140-2 niveau 2
    - Google Cloud KMS

- risques associés KMS & HSM
    - mauvaise config (cf. top 10 OWASP)
    - HSM partitionné n'offre pas de protection entre les partitions

- est il possible de faire de la colocation sur un HSM
    - Commun : multi tenants et partitions
    - Offre OVH "Shared HSM"

- ce qu'est un One time pad
    - Chiffrement théoriquement incassable (prouvé par Shanon)
    - 3 conditions : clé aussi longue que le message, clé vraiment aléatoire, clé détruite/jamais réutilisée
    - sécurité "parfaite" mais impraticable a grande échelle (utilisé pour militaire/diplomatique :ex. téléphone rouge Moscou-Washington) 

- ce qu'est `proxycommand` dans ssh et utiliser ssh pour se connecter sur le darkweb (tor ou i2p)
    - utiliser une commande externe comme tunnel de transport (-J : meme que le rebond ??) 
    - connection via tor (avec une connection tor lancée via `torsock ssh user@xxx.onion`):     ### Via I2P : similaire en pointant vers SOCKS local I2P
```
Host mon-service
    HostName xxxxxxxxxxxxxxxx.onion
    ProxyCommand nc -X 5 -x 127.0.0.1:9050 %h %p
```

- tester `ssh -J -vvv`
    - utilise les clés dans ~/.ssh/config (`Proxyjump hostname`)
    - sinon si clés différentes il faut faire -o ProxyCommand="ssh -i ... bastion"

- chercher l'option identitiesonly dans la config ssh
    - n'utilise que la clé listée dans IdentityFile (et ne teste pas celles dans .ssh)
    - sans ca le serveur recoit plusieurs tentatives de connections par clé avant la bonne, ce qui peut activer le fail2ban

- chercher comment faire ssh X11
    - `ssh -X` ou `ssh -Y` 
    - `X11Forwarding yes` dans `sshd_config`

Pratique :

- deversement des logs chiffrés fail2ban vers l'hote de logs 
- ajouter des métriques LOKI et Prometheus sur l'application et les remonter dans un dashboard grafana
- installation et configuration de suricata sur pfsense
- deversement des logs suricata & pare-feu vers l'hote de logs
- deversement des logs journald vers LOKI
- déployer ALLOY sur les VMs pour remonter les logs systemd vers LOKI
- dans grafana, faire un dashboard qui liste les dernieres alertes fail2ban sur 24h (colonnes IPsource et Hote)

- implémenter un bastion et évaluer le produit sous forme de rapport
    - rapport entre le produit pris et l'autre, ainsi que WallX


Stretch goal :

- chercher un KMS europeen
- ajouter des alertes grafana sur le fail2ban sur telegram
- chercher ce qu'est OPK SSH
- déployer une instance de Zitadel et créer un user connecté à OPK SSH pour que l'user puisse se connecter aux VMs par Zitadel
- analyser ce qu'est Wazuh et faire un déploiement test sur le serveur web avec remontée vers l'hote de logs

